#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int mod = 1e9 + 7;

void solve()
{
    int n, m;
    cin >> n >> m;
    int r[n];
    vector<pair<int, int>> pl;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        pl.push_back(make_pair(x, 0));
    }

    int ans = 0;
    int 

    for (int i = 0; i < m; i++)
    {

    }
}

int main()
{
    // freopen("filename.in","r",stdin);
    // freopen("filename.out","w",stdout);
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;
    cin >> t;
    while (t > 0)
    {
        solve();
        t--;
    }

    return 0;
}